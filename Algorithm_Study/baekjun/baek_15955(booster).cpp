#include <iostream>
using namespace std;

#define SIZE 250000

int main()
{
	int n, q;
	bool booster = false;
	int x[SIZE] = { 0 };
	int y[SIZE] = { 0 };
	cin >> n >> q;

	for (int i = 0; i < n; i++)
	{
		cin >> x[i] >> y[i];
	}
	for (int i = 0; i < q; i++)
	{
		int a, b, hp;
		cin >> a >> b >> hp;


	}

	return 0;
}